import GrowingButtons from "@/components/GrowingButtons";

export default function Home() {
  return (
    <div className="flex items-center justify-center min-h-screen font-[family-name:var(--font-geist-sans)]">
      <GrowingButtons />
    </div>
  );
}
